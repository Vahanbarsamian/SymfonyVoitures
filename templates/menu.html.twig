<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<a class="navbar-brand" href="{{path('accueil')}}">4-Voitures</a>

	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor01">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{path('accueil')}}">Accueil<span class="sr-only">(current)</span>
				</a>
			</li>
			{% if is_granted('ROLE_USER') or is_granted('ROLE_ADMIN') %}
				<li class="nav-item">
					<a class="nav-link" href="{{path('client_cars')}}">Liste des voitures</a>
				</li>
			{% endif %}
		</ul>
		<ul class="navbar-nav ml-auto">
			{% if not app.user %}
				<li class="nav-item">
					<a href="{{path('inscription')}}" class='nav-link'>Inscription</a>
				</li>
			{% endif %}

			{% if not app.user %}
				<li class="nav-item">
					<a href="{{path('login')}}" class='nav-link'>Login</a>
				</li>
			{% endif %}

			{% if app.user %}
				<li class="nav-item">
					<a href="{{path('logout')}}" class='nav-link'>Logout</a>
				</li>
			{% endif %}
			{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item mr-5">
					<a href="{{path('admin_list_cars')}}" class='nav-link'>Admin</a>
				</li>
			{% endif %}
		</ul>
		<form class="form-inline my-2 my-lg-0">
			<input class="form-control mr-sm-2 ml-3" type="text" placeholder="Chercher">
			<button class="btn btn-secondary my-2 my-sm-0 " type="submit">Chercher</button>
		</form>
	</div>
</nav>
